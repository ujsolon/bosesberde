# Frontend Environment Configuration Example
# Copy this file to .env.local and update with your actual values

# API Configuration
NEXT_PUBLIC_API_URL=https://your-api-domain.com

# CORS Origins - should match backend CORS_ORIGINS for consistent security policy
# These domains will be allowed to embed the chatbot and make API calls
CORS_ORIGINS=https://your-main-site.com,https://blog.your-company.com,https://partner-site.com

# Development Example:
# CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000,http://127.0.0.1:3001

# Alternative: Use NEXT_PUBLIC_ prefix if you need client-side access to these values
# NEXT_PUBLIC_CORS_ORIGINS=https://your-main-site.com,https://blog.your-company.com